http.host: 0.0.0.0
transport.host: 0.0.0.0

cluster.name: ${CLUSTER_NAME}

discovery.zen.ping.unicast.hosts: ${DISCOVERY_SERVICE}
discovery.zen.minimum_master_nodes: ${NUM_MASTER_NODES}

node.master: ${MASTER_NODE}
node.data: ${DATA_NODE}
node.ingest: ${INGEST_NODE}
